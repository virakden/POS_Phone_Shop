export enum ApplicationStatus {
    ACTIVE = 'ACTIVE',
    CLOSED = 'CLOSED'
}

export enum ReportType {
    ADJUST_STOCK = 'Adjust Stock',
    DEDUCT_STOCK = 'Adjust Stock',
    SALE = 'Sale'
}

export enum ProductPriority {
    HIGH = 'High',
    MEDIUM = 'Medium',
    LOW = 'Low'
}